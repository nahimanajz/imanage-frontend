const { useState } = require("react");

const filterTable = (data) => {
    const [nameAsQuery, setNameAsQuery] = useState('');
    data.filter(records => records.map(record=> {

    }


    ), );
return (
    <input type="text" placeholder="Search..." />
)
} 